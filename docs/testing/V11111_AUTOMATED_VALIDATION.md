# Quest Chronicle v1.11.11 Automated Validation

## Stamped source result

The version-stamped v1.11.11 source tree passes:

- 121 / 121 Lua regression tests;
- 39 / 39 Python static verifiers;
- 158 / 158 TOC runtime modules parsed successfully;
- 158 unique TOC runtime modules, each listed exactly once;
- 0 runtime Lua files at or above 500 lines.

## v1.11.11-specific fixtures

The new closure fixtures validate:

- prepared anchor coherence and score parity against the frozen scoring oracle;
- exactly one accepted-candidate random draw at the existing boundary;
- zero random draws for rejected incoherent candidates;
- immutable API admission denial;
- anchor candidate synthetic performance, including simulated warm slice and debt limits;
- exact weapon finalist aggregation parity under resumable candidate/relationship scoring;
- exact support bridge decision parity;
- support-profile and beam-node descriptor reuse;
- 24 × 32 support bridge synthetic coverage;
- strict-lower first-best fallback tie parity;
- additive format-4 Zone export diagnostics;
- adaptive-compaction and emergency-stub retention of new anchor/bridge scalar diagnostics.

## Protected inherited wall

All inherited shared-framework, Zone context/evidence, Zone anchor policy, era execution, productive admission, support generation, support reroll, Phase D repair, weapon routing, cache lifecycle, diagnostics, report persistence, export lineage, scheduler integrity, and line-limit tests remain enabled.

Retail performance validation remains authoritative for closure.
